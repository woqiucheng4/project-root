# AI Autonomous Coding Loop Platform

This is a production-grade AI SaaS monorepo powered by Node.js, TypeScript, and PNPM workspaces.

## Folder Structure

- **apps/**: End-user facing applications (e.g., web dashboard, CLI).
- **services/**: Backend microservices (e.g., API gateway, database service).
- **packages/**: Shared logic, domain models, utilities, and UI components.
- **ai-agents/**: Sub-agents responsible for specific cognitive tasks (e.g., planning, coding, reviewing).
- **ai-runtime/**: The core execution loop orchestration that handles state, memory, and LLM integrations.
- **devops/**: Deployment scripts, CI/CD pipelines, and configuration.
- **infrastructure/**: Infrastructure as Code (IaC) like Terraform or Pulumi.
- **docs/**: Architectural documentation, ADRs, and guides. (Includes `docs/prd/` and `docs/architecture/` auto-generated by AI).

## Spec-Driven AI Workflow (BMad Method)

This project incorporates the **BMad Method** principles to prevent LLM hallucinations and enforce context-retention in complex tasks. Our AI agents operate as a virtual agile team:
1. **PMAgent**: Takes vague requirements and outputs a formal Product Requirements Document (PRD).
2. **ArchitectAgent**: Analyzes the PRD to design a visual and technical Architecture Specification.
3. **DevAgent**: Pre-conditioned by both the PRD and Architecture Spec to strictly implement the defined bounds.
4. **QAAgent**: Executes Docker sandbox testing against the generated code.

## Quick Start

```bash
# Install dependencies
pnpm install

# Build all packages
pnpm build

# Run formatting
pnpm format
```
