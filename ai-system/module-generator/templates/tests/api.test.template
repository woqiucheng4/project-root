// API Test Template
// Module: {{ModuleName}}
// Generated by AI Module Generator

import { describe, it, expect } from "vitest";

describe("{{ModuleName}} API", () => {

  it("should create a {{moduleName}}", async () => {
    const res = await fetch("/api/{{routePath}}", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        title: "Test {{ModuleName}}",
        content: "Test content",
      }),
    });
    expect(res.status).toBe(201);
    const data = await res.json();
    expect(data.id).toBeDefined();
    expect(data.title).toBe("Test {{ModuleName}}");
  });

  it("should get all {{moduleName}}s", async () => {
    const res = await fetch("/api/{{routePath}}");
    expect(res.status).toBe(200);
    const data = await res.json();
    expect(Array.isArray(data)).toBe(true);
  });

  it("should get a {{moduleName}} by id", async () => {
    // Create first
    const createRes = await fetch("/api/{{routePath}}", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "Get by ID test", content: "content" }),
    });
    const created = await createRes.json();

    // Get by ID
    const res = await fetch(`/api/{{routePath}}/${created.id}`);
    expect(res.status).toBe(200);
    const data = await res.json();
    expect(data.id).toBe(created.id);
  });

  it("should update a {{moduleName}}", async () => {
    // Create first
    const createRes = await fetch("/api/{{routePath}}", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "Update test", content: "old" }),
    });
    const created = await createRes.json();

    // Update
    const res = await fetch(`/api/{{routePath}}/${created.id}`, {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ content: "updated" }),
    });
    expect(res.status).toBe(200);
  });

  it("should delete a {{moduleName}}", async () => {
    // Create first
    const createRes = await fetch("/api/{{routePath}}", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ title: "Delete test", content: "content" }),
    });
    const created = await createRes.json();

    // Delete
    const res = await fetch(`/api/{{routePath}}/${created.id}`, {
      method: "DELETE",
    });
    expect(res.status).toBe(204);
  });

});
